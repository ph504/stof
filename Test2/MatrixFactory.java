public class MatrixFactory{

	Matrix createMatrix(int row, int column){
		// if there are equal number of rows and columns create a square matrix.
		if(row==column){
			return new SquareMatrix()
		}
	}
}