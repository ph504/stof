class Matrix {
	float [][] matrix;

	float getDeterminant(Matrix mat) {
		// give exception or a log that this isn't a square matrix.
	}
}

class SquareMatrix extends Matrix {

	@override
	getDeterminant(Matrix mat) {
		mat.matrix;
		for(int i=0; i < matrix; ++i) {
			
		}
	}
}